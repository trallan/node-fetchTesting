# node-fetchTesting

### Update Certifcates
`
sudo apt-get update
sudo apt-get install -y ca-certificates
sudo update-ca-certificates
`


### Workaround if certificate gets blocket
- NODE_OPTIONS=--use-openssl-ca